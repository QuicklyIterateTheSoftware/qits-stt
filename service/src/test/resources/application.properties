# The JAX-RS prefix every qits route lives under. A consuming application sets this in its own
# application.properties; SpeechControllerTest asserts against the same absolute paths the monorepo
# used (/api/speech/transcriptions), so the suite sets it too.
quarkus.rest.path=/api
